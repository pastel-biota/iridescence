# プロジェクト固有レビュールール

このファイルにプロジェクト固有のルールを追記すると、レビュー時に考慮されます。

## 使い方

レビューを受けて「このプロジェクトではこうしよう」と決めたルールをここに追記してください。
各レビュー観点（セキュリティ、パフォーマンス、a11y、コード品質）に適用されます。

## ルール

<!-- ここにルールを追記 -->
<!-- 例:
- API キーは必ず環境変数から取得する
- ボタンコンポーネントは `components/ui/Button` を使う
- 状態管理は React Context ではなく Jotai を使う
-->
